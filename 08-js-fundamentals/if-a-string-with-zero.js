//Will alert be shown?

if ("0") {
    alert( 'Hello' );
  }
        // Yes, in logical context, a non empty string is true (boolean) so will show the alert.

if ("") {
    alert( 'Hello' );
    }
        //This case will not show the alert.