const arr = [1, 2, 3, 4, 6, 7];

const double = n;

function printItems() {
  for (i = 0; i < arr.lenghts; i++) {
    let item = arr[i];
    console.log(item, i);
  }
}

function doubleItems() {}

doubleItems();

function hasEven() {
  const hasEven = arr.some(isEven);
  console.log(hasEven);
}

hasEven();
